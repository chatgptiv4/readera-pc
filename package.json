{
  "name": "readera-pc",
  "version": "1.0.0",
  "description": "Browser-based desktop ebook reader inspired by ReadEra",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "dev": "concurrently -n SERVER,CLIENT -c blue,green \"npm run server\" \"npm run client\"",
    "server": "node --experimental-sqlite server/index.js",
    "client": "cd client && npm run dev",
    "setup": "npm install && cd client && npm install",
    "start": "npm run dev"
  },
  "dependencies": {
    "chokidar": "^3.6.0",
    "cors": "^2.8.5",
    "express": "^4.18.3",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
